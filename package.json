{
  "name": "mili_test",
  "private": true,
  "workspaces": {
    "packages": [
      "server-api",
      "web-client"
    ]
  },
  "scripts": {
    "dev": "lerna run --parallel dev"
  },
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
